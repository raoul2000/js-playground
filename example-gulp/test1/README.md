
## Install Gulp

```
# global install (required once)
npm install gulp-cli -g
# install for the current project (devDependencies)
npm install gulp -D
# create gulpFile
touch gulpfile.js

gulp --help
```
